[Unit]
Description=NEST Simulator
Requires=network.target
After=network.target

[Service]
ExecStart=/usr/bin/docker run --rm -dt -p 5000:5000 --name nest-simulator {{ nest_simulator_image }}

[Install]
WantedBy=network.target

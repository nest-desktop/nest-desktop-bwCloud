[Unit]
Description=NEST Server
Requires=network.target
After=network.target

[Service]
ExecStart=/usr/bin/docker run --rm -dt -p 5000:5000 --name nest-server {{ nest_server_image }}

[Install]
WantedBy=network.target
